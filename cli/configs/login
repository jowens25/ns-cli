auth requisite pam_nologin.so
auth required pam_env.so
auth required pam_faillock.so preauth deny=5 silent unlock_time=60
auth sufficient pam_unix.so try_first_pass debug
#auth include /etc/pam.d/ldap/ldap
#auth include /etc/pam.d/tacacs/tacacs
#auth include /etc/pam.d/radius/radius
auth required pam_deny.so
auth required pam_faillock.so authfail deny=5 silent unlock_time=60

account required pam_faillock.so
#account include /etc/pam.d/ldap/ldap
account sufficient pam_unix.so try_first_pass
#account include /etc/pam.d/tacacs/tacacs
#account include /etc/pam.d/radius/radius
account required pam_deny.so

#session include /etc/pam.d/tacacs/tacacs
session required pam_env.so readenv=1
session required pam_motd.so
session required pam_limits.so
session required pam_unix.so

password required pam_unix.so shadow md5